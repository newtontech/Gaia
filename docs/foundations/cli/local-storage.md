# 本地存储

> **Status:** Historical background
>
> **Superseded for Gaia Lang v5 Phase 1:** 当前 Gaia Lang CLI 不包含 `gaia publish --local`，也不再把 LanceDB / Kuzu 本地三写入作为作者侧主流程。当前 canonical workflow 见 [lifecycle.md](lifecycle.md)。

本文档对应更早期的本地数据库实验。它描述的嵌入式 LanceDB / Kuzu 发布流不属于当前 `compile / check / register` 的 source-registry author workflow。
